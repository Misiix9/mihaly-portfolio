# Browsers that we support
# This ensures maximum compatibility while maintaining performance

# Cover 99.5% of users globally
> 0.2%
last 2 versions
not dead

# Specifically support older browsers
IE 11
Chrome >= 60
Firefox >= 55
Safari >= 12
Edge >= 16

# Mobile browsers
iOS >= 12
Android >= 5

# Regional considerations
op_mini all
